# Node_Server

I have created a basic node server and in that I have imported http and using http.createserver created a server and I have passed req->request and res->response as parameters and in this function I have used if and if-else to check the request url that is req.url and based on that i have initilized a basic html page to display for that url and at the end I have defined a variable PORT to run this in localhost and initilized PORT to 3000 and used server.listen to run the server and by passing this PORT variable I am able to run this server in localhost:3000


Note:NO AI is used in this :)
